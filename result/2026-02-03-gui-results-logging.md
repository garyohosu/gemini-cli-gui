# GUI Results + Logging Update (2026-02-03)

- GUI now extracts operations from nested/JSON responses, shows per-operation result column, and writes debug logs to `logs/gui.log`.
- OperationExecutor now emits debug/exception logs.
