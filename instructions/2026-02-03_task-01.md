# Task Instructions (2026-02-03)

- Update `AGENTS.md` to require appending any additional rules here.
- Record the change in `CHANGELOG.md`.
- Create `instructions/` and `result/` folders if missing.
- Provide a result summary in `result/`.
